DETAILED MODULE SPECIFICATIONS1. core/schemas.py (Pydantic v2 Models)Implement strict schemas:NodeScore: Enum (SURE, MAYBE, IMPOSSIBLE).ThoughtNode: id: str, parent_id: Optional[str], thought: str, score: NodeScore, evaluation: str, payload: Dict[str, Any].BayesianPrior: hypothesis: str, prior_prob: float, likelihood: float, evidence_given_not_h: float.FuzzySet: variable_name: str, crisp_value: float, membership_degree: float, label: str.CNFClause: literals: List[str] (e.g., ["A", "NOT_B"]).NegotiationState: agent_utilities: Dict[str, float], disagreement_point: Dict[str, float], pareto_optimal: bool.AgentResponse: agent_id: str, status: str, data: Dict[str, Any], cnf_proof: Optional[List[str]].2. engines/sat_verifier.py (Z3 Solver Integration)Implement class Z3SATVerifier:Method verify_cnf(clauses: List[CNFClause]) -> Tuple[bool, Any]: Converts string clauses into Z3 Boolean variables (z3.Bool), builds z3.Solver(), asserts constraints, and checks solver.check() == z3.sat.Return (True, model) if satisfiable, or (False, unsat_core/conflict_clause) if unsatisfiable.3. engines/nash_negotiator.py (Axiomatic Bargaining)Implement class NashBargainingEngine:Method compute_nash_equilibrium(utility_matrix: np.ndarray, disagreement_point: np.ndarray) -> np.ndarray: Solves $\max \prod (u_i - d_i)$ subject to Pareto efficiency bounds using scipy.optimize.minimize.Method compute_kalai_smorodinsky(...): Implements proportional line intersection from disagreement point to utopia point.4. engines/bayes_fuzzy.py (Uncertainty Processing)Implement class UncertaintyEngine:Method update_bayes(prior: BayesianPrior) -> float: Computes posterior $P(H|E) = \frac{P(E|H)P(H)}{P(E)}$.Method defuzzify_centroid(memberships: List[Tuple[float, float]]) -> float: Calculates Center of Gravity (CoG) $\frac{\sum \mu(z) \cdot z}{\sum \mu(z)}$.5. engines/cbr_default.py (Non-Monotonic & CBR)Implement class AdaptiveMemoryEngine:Method cbr_retrieve(query_features: Dict[str, float], case_base: List[Dict]) -> Dict: Calculates weighted Euclidean distance.Method apply_default_rule(fact_base: Set[str], rule: Dict) -> Set[str]: Evaluates $\frac{\alpha : \beta}{\gamma}$. If $\neg \beta$ enters fact_base, triggers AGM retraction and returns updated belief set.6. engines/causal_abduction.py (Do-Calculus & Abduction)Implement class CausalAbductionEngine:Method abductive_diagnosis(observations: List[str], causal_map: Dict[str, List[str]]) -> List[str]: Finds minimal cover set $|H'|$ explaining all observations (Occam's Razor).Method apply_do_operator(graph: nx.DiGraph, intervention_var: str) -> nx.DiGraph: Removes all incoming edges to intervention_var for Pearl's $do()$ intervention.7. pipeline/iso_704_normalizer.py (Linguistic Normalization)Implement class ISO704Normalizer:Method normalize_text(raw_text: str) -> Dict: Performs UTF-8 cleaning, POS Tagging (via SpaCy/NLTK), extracts Noun Phrases, and formats outputs into RDF/OWL triples [Subject, Predicate, Object].8. agents/system_prompts.pyEmbed the exact, complete system prompts for:SYSTEM_PROMPT_MASTER_ORCHESTRATOR (Agent 0: ToT / MCTS)SYSTEM_PROMPT_PERCEPTRON (Agent 1: Bayes / Fuzzy / Do-Calculus)SYSTEM_PROMPT_MEMORY (Agent 2: Default Logic / CBR / Abduction)SYSTEM_PROMPT_VERIFIER (Agent 3: Z3 SAT / Nash Bargaining / ISO 704)9. agents/master_orchestrator.py (LangGraph Integration)Implement class MasterOrchestrator:Builds a StateGraph in LangGraph.Executes Tree of Thoughts (ToT) exploration.Dispatches sub-tasks asynchronously to Perceptron, Memory, and Verifier agents.Prunes branches immediately if VerifierAgent returns NodeScore.IMPOSSIBLE (Z3 UNSAT).Performs backtracking to the nearest MAYBE node if a path stalls.10. main.py & tests/main.py: FastAPI application exposing POST /api/v1/solve endpoint that accepts complex systemic queries and returns full JSON execution trace.Complete PyTest suite covering Z3 SAT verification, Nash bargaining optimization, and end-to-end orchestrator execution.⚡ EXECUTION DIRECTIVE FOR THE IDECreate directory structure immediately.Generate all dependencies in requirements.txt.Write core/schemas.py and ensure type-safety.Write reasoning modules under engines/ with robust mathematical algorithms.Implement agents and LangGraph DAG in agents/ and pipeline/.Write tests in tests/ and ensure all test cases pass cleanly.Provide Dockerfile and docker-compose.yml for instant deployment.